// Predict and explain first...
// return; on it own is not a command. What is it that you want the function to return?
// It must be stated... The parameters of sum are a and b.
// There inside of your functions parentheses it should say return a + b;

function sum(a, b) {
  return a + b;
}

console.log(`The sum of 10 and 32 is ${sum(10, 32)}`);
console.log(`The sum of 9 and 10 is ${sum(9, 10)}`);
