// Predict and explain first...
// this function won't return anything because of the return has nothing this may cause an error or an defined result.
// to make the function work we change the return and the 'a+b' under it into 1 line all together.

function sum(a, b) {
  return a + b;
}

console.log(`The sum of 10 and 32 is ${sum(10, 32)}`);
