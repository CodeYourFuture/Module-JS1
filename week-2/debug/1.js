// Predict and explain first...

function sum(a, b) {
  return a + b;
  
}

console.log(`The sum of 10 and 32 is ${sum(10, 32)}`);

// This function will not work because there is a semi colon after the return which should not be there. With the semi-colon immediately after the return it will return undefined as the answer. 