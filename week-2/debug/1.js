// Predict and explain first...

  // function will not work properly because of the semicolon between return and the addition

  // to fix it we need to delete the semicolon after return method

function sum(a, b) {
  return a + b;
}

console.log(`The sum of 10 and 32 is ${sum(10, 32)}`);
