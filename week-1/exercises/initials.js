let firstName = "Creola";
let middleName = "Katherine";
let lastName = "Johnson";
let initials = (firstName.substring(0,3)).toUpperCase()  + " " + (middleName.substring(0,3)).toUpperCase() + " "+ (lastName.substring(0,3)).toUpperCase() 

// Declare a variable called initials that stores the first 3 characters of each string in upper case
// Log the variable in each case

//Ele quer os 3 primeiros caracteres de cada, para adicionar espaco use aspas, coloca em maiusculas to Uppercase


console.log (initials)
