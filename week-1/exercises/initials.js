//let initials = firstName.substring(0, 3).toUpperCase();
//console.log(initials);
let firstName = "Creola";
let upname = firstName.substring(0, 3).toUpperCase();
console.log(upname);

let middleName = "Katherine";
let mName = middleName.substring(0, 3).toUpperCase();
console.log(mName);

let lastName = "Johnson";
let lName = lastName.substring(0, 3).toUpperCase();
console.log(lName);

let initials = upname + mName + lName;
console.log(initials);

// Declare a variable called initials that stores the first 3 characters of each string in upper case
// Log the variable in each case
