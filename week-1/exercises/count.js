let count = 0;

count = count + 1;
console.log(count);

// Line 1 is a variable declaration, creating the count variable with an initial value of 0
// Describe what line 3 is doing, in particular focus on what = is doing

// Line 3 is increasing the variable count by a value of 1.
// The sign '=' is assigning value (i.e. count + 1) to the variable, count.
